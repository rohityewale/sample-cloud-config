spring:
  application:
    name: online-service
  #data:
  #  mongodb:
   #   host: localhost
   #   database: online-db
   #   port: 27017
server:
  port: 8081


eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true